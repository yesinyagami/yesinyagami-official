import { Outlet } from 'react-router-dom';
import { LayoutGroup } from 'framer-motion'; // 假設使用 Framer Motion
import MeasureLayout from './MeasureLayout.mjs'; // 調整路徑

function Layout({ children }) {
  return (
    <LayoutGroup>
      <MeasureLayout>
        <div>
          <h1>Tarot App Layout</h1>
          <Outlet /> {/* 渲染子路由，如 LoginPage */}
        </div>
      </MeasureLayout>
    </LayoutGroup>
  );
}

export default Layout;